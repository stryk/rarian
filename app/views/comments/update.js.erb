$("#comment_item_<%= @commentable_obj.class.name.downcase + @commentable_obj.id.to_s + @comment.id.to_s %>").hide('slow')

$("#comment_item_<%= @commentable_obj.class.name.downcase + @commentable_obj.id.to_s + @comment.id.to_s %>").html('<%=j render :partial => 'shared/comment_item', :locals => {:comment => @comment, :comment_obj => @commentable_obj} %>').show('slow');
