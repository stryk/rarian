<div id="add_blip" class="modal hide fade" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel"><%= @company.name %></h3>
  </div>
  <div class="modal-body">
    <%= form_for(Blip.new, :html => {:class => 'form-vertical' }, :remote => true) do |f| %>
         <% f.select :action, "Action",  %>
        <%= f.button :submit, 'Submit', id: 'create_blip', :class => 'btn-primary' %>
    <% end %>
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Close</button>
    <button class="btn btn-primary">Save changes</button>
  </div>
</div>