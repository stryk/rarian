$('#company_follow').html("<%= j render :partial => 'shared/follow_buttons', :locals => {:obj => @company,
                                                                                          :follow_link => follow_company_path(@company),
                                                                                          :unfollow_link => unfollow_company_path(@company),
                                                                                          :big_small => 'big'} %>");